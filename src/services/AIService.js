/**
 * AIService Placeholder for future AI integration phase.
 * Currently disabled as per Phase 1 rule-based automation specifications.
 */

const analyzeIntent = async (message) => {
  throw new Error('AI Engine is currently disabled in Phase 1');
};

const generateResponse = async (prompt) => {
  throw new Error('AI Engine is currently disabled in Phase 1');
};

module.exports = {
  analyzeIntent,
  generateResponse
};
